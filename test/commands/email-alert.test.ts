import {run} from './apex-command-runner.js';

const NAME = 'Email_Alert';
const PATH = 'data-test\\elements\\';

describe('apex', () => {
    // "Email Alert" element
    run(NAME, PATH);
});
